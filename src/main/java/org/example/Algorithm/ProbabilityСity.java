package org.example.Algorithm;

import org.example.VKData.UserDB;

import java.util.ArrayList;
import java.util.Map;
import java.util.TreeMap;

public class ProbabilityСity {
    public static ArrayList<Probability.Node> getCityGenerate(int id, int level, int percent) {
        TreeMap<Integer, Integer> map = new TreeMap<>();
        ArrayList<Probability.Node> buffer = new ArrayList<>();

        return buffer;
    }
}
